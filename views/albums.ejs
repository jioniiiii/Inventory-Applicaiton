<!-- maybe remake card so car is not the whole card but its has linkfor genre and label etc -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Albums</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 0;
      color: #333;
    }

    h1 {
      text-align: center;
      margin: 20px 0;
      color: #444;
    }

    ul {
      list-style-type: none;
      padding: 0;
      margin: 0;
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
    }

    .album {
      background: #fff;
      border-radius: 8px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      margin: 10px;
      padding: 15px;
      width: 250px;
      text-align: center;
      transition: box-shadow 0.3s ease;
    }

    .album img {
      max-width: 100%;
      height: auto;
      border-radius: 4px;
    }

    .album h2 {
      font-size: 1.2em;
      margin: 10px 0;
      color: #222;
    }

    .album p {
      margin: 5px 0;
      font-size: 0.9em;
    }

    .album strong {
      color: #555;
    }

    .album:hover {
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
    }
    a {
            color: black;
            text-decoration: none;
        }
  </style>
</head>
<body>
  <h1>Albums</h1>
  <ul>
    <% albums.forEach(album => { %>
      <a href="/album/<%= encodeURIComponent(album.title) %>">
        <li class="album">
          <% if (album.image) { %>
            <img src="<%= album.image %>" alt="<%= album.title %>">
          <% } %>
          <h2><%= album.title %></h2>
          <p><strong>Released:</strong> <%= album.release_date.toDateString() %></p>
          <p><strong>Artist:</strong> <%= album.artist.name %></p>
          <p><strong>Genre:</strong> <%= album.genre ? album.genre.name : 'N/A' %></p>
          <p><strong>Label:</strong> <%= album.label ? album.label.name : 'N/A' %></p>
        </li>
      </a>
    <% }) %>
  </ul>
</body>
</html>