<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= genre.name %> - Albums</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <%- include('../partials/nav') %>
    <div class="genre-info">
        <h1>Albums by <%= genre.name %></h1>
        <p>Founded: <%= genre.founded ? genre.founded : 'N/A' %></p>
    </div>
    <div class="albums-grid">
        <% albums.forEach(album => { %>
            <div class="album-card">
                <a href="/album/<%= encodeURIComponent(album.title) %>">
                    <img src="<%= album.image %>" alt="<%= album.title %>">
                    <div class="album-info">
                        <h3><%= album.title %></h3>
                        <p><strong>Artist:</strong> <%= album.artist.first_name %> <%= album.artist.last_name %></p>
                        <p><strong>Release Date:</strong> <%= album.release_date_formatted %></p>
                    </div>
                </a>
            </div>
        <% }) %>
    </div>
</body>
</html>