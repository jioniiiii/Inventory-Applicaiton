<!--  add nav add the format model for buying here make it pretty -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= album.title %> - Details</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 20px;
            color: #333;
        }
        .album-details {
            max-width: 800px;
            margin: auto;
            background: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .album-details img {
            width: 100%;
            height: auto;
            border-radius: 10px;
        }
        .album-info {
            padding: 20px 0;
        }
        h1, p {
            margin: 0 0 10px;
        }
        a {
            color: black;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <div class="album-details">
        <img src="<%= album.image %>" alt="<%= album.title %>">
        <div class="album-info">
            <h1><%= album.title %></h1>
            <p><strong>Released:</strong> <%= album.release_date.toDateString() %></p>
            <p><strong>Artist:</strong> <%= album.artist.name %></p>
            <p><strong>Genre:</strong> <%= album.genre ? album.genre.name : 'N/A' %></p>
            <p><strong>Label:</strong> <%= album.label ? album.label.name : 'N/A' %></p>
        </div>
    </div>
</body>
</html>